# Vercel Environment Variables
# Copy these to your Vercel dashboard under Settings > Environment Variables

NODE_ENV=production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-vercel
DB_PATH=/tmp/course_management.db
UPLOAD_PATH=/tmp/uploads

# Note: In Vercel, files in /tmp are temporary and will be reset on each deployment
# For persistent storage, consider using external services like:
# - Vercel Blob for file uploads
# - PlanetScale/Supabase for database
# - Or implement a cloud database solution
